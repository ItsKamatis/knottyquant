---
interface Props {
  label?: string;
}

const { label = 'Data boundary' } = Astro.props;
---

<aside class="data-boundary" aria-label={label}>
  <strong>{label}</strong>
  <div><slot /></div>
</aside>
